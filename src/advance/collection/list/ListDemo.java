package advance.collection.list;

import java.util.*;

/**
 * <h1>List (Ordered Collection)</h1>
 * <p>Elements can occur more than once in a list, hence contain duplicates
 * <p>Preserves insertion order of elements, hence elements can be accessed by their index
 * <p>Following classes implements List interface:
 * <ol>
 * <li>ArrayList</li>
 * <li>LinkedList</li>
 * <li>Vector</li>
 * <li>Stack</li>
 * </ol>
 */
public class ListDemo {
    List<Integer> arraylist = new ArrayList<>();
    List<Integer> linkedList = new LinkedList<>();
    List<Integer> vector = new Vector<>();
    List<Integer> stack = new Stack<>();
}
